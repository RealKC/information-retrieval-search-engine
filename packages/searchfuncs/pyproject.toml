[project]
name = "searchfuncs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Dumitru Mițca", email = "dumitru0mitca@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "indexing",
    "stemmer",
    "trie",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
indexing = { workspace = true }
stemmer = { workspace = true }
trie = { workspace = true }
